apiVersion: storage.azure.upbound.io/v1beta2
kind: Account
metadata:
  name: stcpvalidatecloud01
spec:
  forProvider:
    resourceGroupName: rg-sdbx-np-dev
    location: australiaeast
    accountTier: Standard
    accountReplicationType: LRS
    minTlsVersion: TLS1_2
    tags:
      Environment: devtest
      Purpose: cloud-plan-test
      ManagedBy: crossplane
  providerConfigRef:
    name: platform-devtest
